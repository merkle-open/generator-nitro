language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sentinel' ]; then cd .. && eval "mv $currentfolder generator-sentinel" && cd generator-sentinel; fi
