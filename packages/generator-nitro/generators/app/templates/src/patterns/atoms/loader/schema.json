{
	"$schema": "http://json-schema.org/schema#",
	"title": "Loader",
	"description": "A Loader atom",
	"type": "object",
	"properties": {
		"modifier": {
			"description": "A modifier",
			"enum": ["", "white"]
		}
	}
}
