# Example

This is an example.
It shows a basic example of a pattern:

- with a [visual modifier](./css/modifier/example-highlight.scss)<% if (options.clientTpl) { %>
- with example usage of clientside templates with handlebarsJS<% } %>
