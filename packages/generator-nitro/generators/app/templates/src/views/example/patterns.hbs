<div class="container">
	<div class="row">
		<div class="col-md-8 offset-md-2">
			{{pattern name='gondel'}}
			{{pattern name='heading' tag='h1'}}
			{{pattern name='example'}}
			{{#pattern name='box'}}
				<h3>{{pattern name='icon' icon='nitro'}}&nbsp;Nitro example patterns</h3>
				{{pattern name='button'}}
				{{pattern name='checkbox'}}
			{{/pattern}}
			{{pattern name='box' modifier='dark'}}
			{{pattern name='cta'}}
			{{#pattern name='box'}}
				{{pattern name='loader'}}
			{{/pattern}}
			<div class="box" style="outline: 0.5vw solid #b2b2b2;">
				{{pattern name='image'}}
			</div>
			{{pattern name='list' data='mix'}}
		</div>
	</div>
</div>
<div class="container-fluid">
	<div class="row">
	{{pattern name='stage'}}
	</div>
</div>
