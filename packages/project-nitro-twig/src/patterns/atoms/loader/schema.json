{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Loader",
	"description": "A Loader atom",
	"type": "object",
	"properties": {
		"modifier": {
			"description": "A modifier",
			"enum": ["", "white"]
		}
	}
}
