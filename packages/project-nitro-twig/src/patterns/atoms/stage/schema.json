{
	"$schema": "http://json-schema.org/schema#",
	"title": "stage",
	"description": "A stage atom",
	"type": "object",
	"properties": {
		"heading": {
			"description": "a heading pattern",
			"type": "object"
		}
	}
}
