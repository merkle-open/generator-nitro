<div class="t-ex{{#if modifier}} t-ex--{{modifier}}{{/if}}" data-t-name="Ex"{{#if decorator}} data-t-decorator="{{decorator}}"{{/if}}>
	<h2 class="t-ex__headline">2. Template - {{title}}</h2>
	<ul>
		{{#each links}}
		{{pattern 'Ex-link' data=this}}
		{{/each}}
	</ul>
</div>
