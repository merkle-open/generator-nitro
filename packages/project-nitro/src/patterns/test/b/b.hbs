<div class="a-b{{#if modifier}} a-b--{{modifier}}{{/if}}" data-t-name="B"{{#if decorator}} data-t-decorator="{{decorator}}"{{/if}}>
	<h2>{{headline.title}} {{headline.count}}</h2>
	{{pattern name='a' data=this}}
</div>
